const [FoodBoxList, setFoodBoxList] = useState();
let updatedFoodBoxList = [...FoodBoxList];

function FeedBack();
if (FoodBoxList.length === 0)
 {console.log("Oops! There is no more content to show") }

export default FeedBack;
